import React from 'react';
import { createRoot } from 'react-dom/client';
import GrantSearch from './GrantSearch';

const root = createRoot(document.getElementById('root'));
root.render(<GrantSearch />);